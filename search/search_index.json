{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"vehicle-keypoints","text":"<p>Production-grade 14-keypoint vehicle pose estimation on the CarFusion (CMU) dataset \u2014 four wheels, four head- and tail-lights, four roof corners, the exhaust, and a body-centre reference point per car.</p> <p></p>"},{"location":"#task","title":"Task","text":"<p>Per-vehicle 2D landmark localisation on RGB images. 14 anatomical keypoints per car instance follow the original CarFusion (Reddy et al., CVPR 2018) schema: 4 wheels (front/rear \u00d7 left/right), 4 head- and tail-lights (front/rear \u00d7 left/right), 4 roof corners (front/rear \u00d7 left/right), the exhaust, and a body-centre reference point. Each keypoint carries an <code>(x, y, v)</code> triplet where <code>v \u2208 {0, 1, 2}</code> (absent / occluded / visible) following the COCO convention.</p>"},{"location":"#models","title":"Models","text":"<ul> <li>Main \u2014 YOLO26-pose (Ultralytics 8.x). Anchor-free single-shot detector + keypoint head; non-human-keypoint-friendly, high throughput, standard <code>.pt</code> export.</li> <li>Baseline \u2014 ViTPose-S (HF <code>transformers</code> 5.x, Lightning-wrapped). Top-down (crop \u2192 heatmap) transformer; pretrained on COCO human 17-kpt, re-headed to 14 car keypoints.</li> </ul>"},{"location":"#stack","title":"Stack","text":"<p>Python 3.13 \u00b7 uv (cu124 torch wheels) \u00b7 Ultralytics 8.x \u00b7 transformers 5.x \u00b7 PyTorch Lightning \u00b7 Hydra \u00b7 MLflow (ViTPose branch) \u00b7 FastAPI \u00b7 Docker \u00b7 MkDocs Material \u00b7 GitHub Actions.</p>"},{"location":"#navigation","title":"Navigation","text":"<ul> <li>Architecture \u2014 data flow, two-branch pipeline, metric choices</li> <li>Training \u2014 YOLO + ViTPose commands, Hydra overrides, GPU notes</li> <li>Serving \u2014 FastAPI <code>/detect</code>, Docker, JSON + PNG overlay responses</li> <li>Model card \u2014 HF Hub card template (Jinja-rendered at publish time)</li> </ul>"},{"location":"#links","title":"Links","text":"<ul> <li>Code: https://github.com/kiselyovd/vehicle-keypoints</li> <li>Model: https://huggingface.co/kiselyovd/vehicle-keypoints</li> </ul>"},{"location":"#intended-use","title":"Intended use","text":"<p>Research and educational artifact demonstrating modern keypoint-detection pipelines on a non-human class. Not intended for any safety-critical, autonomous-driving, or surveillance deployment \u2014 the model is trained on a single academic dataset and has not been validated for production use.</p>"},{"location":"api/","title":"API Reference","text":"<p>Auto-generated reference for the <code>vehicle_keypoints</code> package via mkdocstrings. Each module below lists its public classes and functions with their docstrings and type signatures.</p>"},{"location":"api/#package-root","title":"Package root","text":""},{"location":"api/#vehicle_keypoints","title":"vehicle_keypoints","text":"<p>Production-grade vehicle keypoint detection (14 anatomical car keypoints, CarFusion).</p>"},{"location":"api/#data","title":"Data","text":""},{"location":"api/#vehicle_keypoints.data.coco_dataset","title":"coco_dataset","text":"<p>Top-down COCO-keypoints dataset for ViTPose training.</p> <p>Yields <code>(crop_tensor, target_heatmap, visibility)</code> for each GT car instance. Crop is extracted from <code>bbox</code> (with margin) and resized to a fixed (H, W). Heatmap targets are Gaussian blobs at GT keypoint locations in the crop.</p>"},{"location":"api/#vehicle_keypoints.data.coco_dataset-classes","title":"Classes","text":""},{"location":"api/#vehicle_keypoints.data.coco_dataset.CocoKeypointsDataset","title":"CocoKeypointsDataset","text":"<pre><code>CocoKeypointsDataset(\n    images_root: Path | str,\n    annotations_json: Path | str,\n    crop_hw: tuple[int, int] = DEFAULT_CROP,\n    heatmap_hw: tuple[int, int] = DEFAULT_HEATMAP,\n    margin: float = 0.1,\n)\n</code></pre> <p>               Bases: <code>Dataset</code></p> <p>Iterate per-instance (one crop per annotation).</p> Source code in <code>src/vehicle_keypoints/data/coco_dataset.py</code> <pre><code>def __init__(\n    self,\n    images_root: Path | str,\n    annotations_json: Path | str,\n    crop_hw: tuple[int, int] = DEFAULT_CROP,\n    heatmap_hw: tuple[int, int] = DEFAULT_HEATMAP,\n    margin: float = 0.1,\n) -&gt; None:\n    self.images_root = Path(images_root)\n    self.crop_hw = crop_hw\n    self.heatmap_hw = heatmap_hw\n    self.margin = margin\n    raw: dict[str, Any] = json.loads(Path(annotations_json).read_text(encoding=\"utf-8\"))\n    self.images_by_id = {img[\"id\"]: img for img in raw[\"images\"]}\n    self.annotations = [a for a in raw[\"annotations\"] if a[\"num_keypoints\"] &gt; 0]\n</code></pre>"},{"location":"api/#vehicle_keypoints.data.datamodule","title":"datamodule","text":"<p>Lightning DataModule for ViTPose baseline (COCO-style top-down pose).</p>"},{"location":"api/#vehicle_keypoints.data.datamodule-classes","title":"Classes","text":""},{"location":"api/#vehicle_keypoints.data.prepare","title":"prepare","text":"<p>Prepare COCO -&gt; Ultralytics YOLO-format dataset layout.</p>"},{"location":"api/#models","title":"Models","text":""},{"location":"api/#vehicle_keypoints.models.factory","title":"factory","text":"<p>Model factory -- main YOLO26-pose + baseline ViTPose-S.</p>"},{"location":"api/#vehicle_keypoints.models.factory-functions","title":"Functions","text":""},{"location":"api/#vehicle_keypoints.models.factory.build_model","title":"build_model","text":"<pre><code>build_model(name: str, num_keypoints: int, pretrained: bool = True) -&gt; Any\n</code></pre> <p>Return either an <code>ultralytics.YOLO</code> or a <code>torch.nn.Module</code>.</p> <p>YOLO path (name startswith \"yolo\"):   - loads an Ultralytics <code>.pt</code> (pretrained on COCO human pose; we fine-tune the head     on CarFusion's 14-kpt layout at train time). ViTPose path (name startswith \"vitpose\"):   - returns a <code>ViTPoseSmall</code> nn.Module emitting heatmaps of shape (B, num_keypoints, H', W').</p> Source code in <code>src/vehicle_keypoints/models/factory.py</code> <pre><code>def build_model(name: str, num_keypoints: int, pretrained: bool = True) -&gt; Any:\n    \"\"\"Return either an `ultralytics.YOLO` or a `torch.nn.Module`.\n\n    YOLO path (name startswith \"yolo\"):\n      - loads an Ultralytics `.pt` (pretrained on COCO human pose; we fine-tune the head\n        on CarFusion's 14-kpt layout at train time).\n    ViTPose path (name startswith \"vitpose\"):\n      - returns a `ViTPoseSmall` nn.Module emitting heatmaps of shape (B, num_keypoints, H', W').\n    \"\"\"\n    if name.startswith(\"yolo\"):\n        from ultralytics import YOLO\n\n        if pretrained:\n            for candidate in (f\"{name}-pose.pt\", *YOLO_FALLBACKS):\n                try:\n                    return YOLO(candidate)\n                except (FileNotFoundError, Exception):\n                    continue\n            raise ValueError(f\"No pretrained pose checkpoint found for {name} (tried fallbacks)\")\n        return YOLO(f\"{name}-pose.yaml\")\n\n    if name.startswith(\"vitpose\"):\n        from .vitpose import ViTPoseSmall\n\n        return ViTPoseSmall(num_keypoints=num_keypoints, pretrained=pretrained)\n\n    raise ValueError(f\"Unknown model: {name}\")\n</code></pre>"},{"location":"api/#vehicle_keypoints.models.lightning_module","title":"lightning_module","text":"<p>Lightning wrapper for ViTPose (heatmap-regression head).</p>"},{"location":"api/#vehicle_keypoints.models.lightning_module-classes","title":"Classes","text":""},{"location":"api/#vehicle_keypoints.models.lightning_module.KeypointsModule","title":"KeypointsModule","text":"<pre><code>KeypointsModule(\n    model: Module,\n    num_keypoints: int,\n    lr: float = 0.0005,\n    model_name: str | None = None,\n)\n</code></pre> <p>               Bases: <code>LightningModule</code></p> <p>Heatmap-regression Lightning module for top-down pose estimation.</p> <p>Forward expects a (B, 3, H, W) crop. Output is (B, K, H', W') heatmaps. Training target is a set of Gaussian-blobbed heatmaps centered on GT kpt locations. Loss is MSE, masked by keypoint visibility.</p> Source code in <code>src/vehicle_keypoints/models/lightning_module.py</code> <pre><code>def __init__(\n    self,\n    model: nn.Module,\n    num_keypoints: int,\n    lr: float = 5e-4,\n    model_name: str | None = None,\n) -&gt; None:\n    super().__init__()\n    self.model = model\n    self.num_keypoints = num_keypoints\n    self.save_hyperparameters(ignore=[\"model\"])\n</code></pre>"},{"location":"api/#vehicle_keypoints.models.vitpose","title":"vitpose","text":"<p>ViTPose-Small wrapper returning heatmap predictions for N keypoints.</p>"},{"location":"api/#vehicle_keypoints.models.vitpose-classes","title":"Classes","text":""},{"location":"api/#vehicle_keypoints.models.vitpose.ViTPoseSmall","title":"ViTPoseSmall","text":"<pre><code>ViTPoseSmall(num_keypoints: int = 14, pretrained: bool = True)\n</code></pre> <p>               Bases: <code>Module</code></p> <p>Thin wrapper around HF VitPose model, re-headed to N keypoints.</p> <p>We use <code>usyd-community/vitpose-small-simple</code> as the pretrained backbone (ImageNet + MS-COCO human pose). The head is replaced to emit <code>num_keypoints</code> heatmaps.</p> Source code in <code>src/vehicle_keypoints/models/vitpose.py</code> <pre><code>def __init__(self, num_keypoints: int = 14, pretrained: bool = True) -&gt; None:\n    super().__init__()\n    from transformers import VitPoseConfig, VitPoseForPoseEstimation\n\n    model_id = \"usyd-community/vitpose-small-simple\"\n    if pretrained:\n        try:\n            self.backbone = VitPoseForPoseEstimation.from_pretrained(\n                model_id, num_labels=num_keypoints, ignore_mismatched_sizes=True\n            )\n        except Exception:\n            cfg = VitPoseConfig(num_labels=num_keypoints)  # type: ignore[call-arg]\n            self.backbone = VitPoseForPoseEstimation(cfg)\n    else:\n        cfg = VitPoseConfig(num_labels=num_keypoints)  # type: ignore[call-arg]\n        self.backbone = VitPoseForPoseEstimation(cfg)\n    self.num_keypoints = num_keypoints\n</code></pre>"},{"location":"api/#training","title":"Training","text":""},{"location":"api/#vehicle_keypoints.training.train","title":"train","text":"<p>Training entrypoint (Hydra-powered).</p>"},{"location":"api/#vehicle_keypoints.training.train_vitpose","title":"train_vitpose","text":"<p>ViTPose baseline training entrypoint (Lightning + Hydra).</p> <p>Separate from <code>train.py</code> \u2014 the main YOLO path bypasses Lightning entirely and is handled by Ultralytics' own training loop in train.py.</p>"},{"location":"api/#evaluation","title":"Evaluation","text":""},{"location":"api/#vehicle_keypoints.evaluation.evaluate","title":"evaluate","text":"<p>Evaluate pose predictions against COCO-keypoints GT.</p> Supports two inputs <ol> <li>YOLO checkpoint (.pt)  - auto-predicts over data/processed/images/test/</li> <li>Precomputed predictions JSON (COCO result format)</li> </ol> <p>Metrics: OKS-mAP via pycocotools + PCK@0.05 (per-keypoint-correct within 5% of bbox diagonal).</p>"},{"location":"api/#vehicle_keypoints.evaluation.evaluate-classes","title":"Classes","text":""},{"location":"api/#inference","title":"Inference","text":""},{"location":"api/#vehicle_keypoints.inference.overlay","title":"overlay","text":"<p>Render keypoints + skeleton overlays on input images (CPU, OpenCV).</p>"},{"location":"api/#vehicle_keypoints.inference.predict","title":"predict","text":"<p>Inference helper: load YOLO pose checkpoint and run detection.</p>"},{"location":"api/#vehicle_keypoints.inference.predict-classes","title":"Classes","text":""},{"location":"api/#vehicle_keypoints.inference.predict.Detector","title":"Detector  <code>dataclass</code>","text":"<pre><code>Detector(model: Any)\n</code></pre> <p>Thin wrapper around an ultralytics.YOLO predictor.</p>"},{"location":"api/#vehicle_keypoints.inference.predict.Detector-functions","title":"Functions","text":"from_pretrained_or_random <code>classmethod</code> <pre><code>from_pretrained_or_random(base_name: str = 'yolo26n') -&gt; Detector\n</code></pre> <p>Factory used in tests \u2014 loads pretrained pose <code>.pt</code> if available, else YAML.</p> Source code in <code>src/vehicle_keypoints/inference/predict.py</code> <pre><code>@classmethod\ndef from_pretrained_or_random(cls, base_name: str = \"yolo26n\") -&gt; Detector:\n    \"\"\"Factory used in tests \u2014 loads pretrained pose `.pt` if available, else YAML.\"\"\"\n    from ultralytics import YOLO\n\n    for candidate in (f\"{base_name}-pose.pt\", \"yolo11n-pose.pt\", f\"{base_name}-pose.yaml\"):\n        try:\n            return cls(model=YOLO(candidate))\n        except Exception:  # nosec B112 - intentional fallback over candidate list\n            continue\n    raise RuntimeError(f\"Could not instantiate YOLO for {base_name}\")\n</code></pre>"},{"location":"api/#serving","title":"Serving","text":""},{"location":"api/#vehicle_keypoints.serving.dependencies","title":"dependencies","text":"<p>FastAPI DI \u2014 lazy-loaded Detector singleton.</p>"},{"location":"api/#vehicle_keypoints.serving.dependencies-classes","title":"Classes","text":""},{"location":"api/#vehicle_keypoints.serving.errors","title":"errors","text":"<p>Exception types and handlers.</p>"},{"location":"api/#vehicle_keypoints.serving.main","title":"main","text":"<p>FastAPI application.</p>"},{"location":"api/#vehicle_keypoints.serving.routes","title":"routes","text":"<p>FastAPI routes for the pose detection service.</p>"},{"location":"api/#vehicle_keypoints.serving.routes-classes","title":"Classes","text":""},{"location":"api/#vehicle_keypoints.serving.schemas","title":"schemas","text":"<p>Pydantic schemas for the /detect endpoint.</p>"},{"location":"api/#script-helpers","title":"Script helpers","text":""},{"location":"api/#vehicle_keypoints.scripts_lib.convert_carfusion","title":"convert_carfusion","text":"<p>Convert raw CarFusion (CMU) dumps to COCO keypoints JSON.</p> <p>Reusable functions (import-friendly for tests); a thin CLI wrapper lives at <code>scripts/convert_carfusion_to_coco.py</code>.</p> <p>Input layout (raw CarFusion):     raw_dir//gt/.txt  \u2014 per-frame keypoint rows     raw_dir///.jpg <p>Each <code>.txt</code> row has 5 comma-separated fields:     x, y, keypoint_id(1..14), instance_id, visibility(1|2|3)</p> <p>CarFusion visibility convention -&gt; COCO visibility:     1 (visible)          -&gt; 2 (labeled + visible)     2 (labeled occluded) -&gt; 1 (labeled but not visible)     3 (occluded)         -&gt; 2 (labeled + visible)  # legacy script treated 3 as 1     other                -&gt; 0 (not labeled)</p>"},{"location":"api/#utilities","title":"Utilities","text":""},{"location":"api/#vehicle_keypoints.utils.hf_hub","title":"hf_hub","text":"<p>HuggingFace Hub helpers.</p>"},{"location":"api/#vehicle_keypoints.utils.logging","title":"logging","text":"<p>Structured logging configuration.</p>"},{"location":"api/#vehicle_keypoints.utils.seed","title":"seed","text":"<p>Deterministic seeding across libraries.</p>"},{"location":"architecture/","title":"Architecture","text":"<p>Two independent branches that share the same CarFusion COCO export but diverge at the dataset layer: YOLO consumes a processed YOLO-layout tree via <code>data.yaml</code>, while ViTPose reads the COCO JSON directly and renders Gaussian heatmaps per visible keypoint.</p>"},{"location":"architecture/#data-flow","title":"Data flow","text":"<pre><code>flowchart LR\n  Src[CarFusion COCO&lt;br/&gt;D:/ProjectsData/Car Key Point]:::external --&gt; Sync[scripts/sync_data.sh]:::code\n  Sync --&gt; Raw[\"data/raw/{annotations,train,test}&lt;br/&gt;gitignored\"]:::data\n  Raw --&gt; Prep[data/prepare.py&lt;br/&gt;scene-level 90/10 split]:::code\n  Prep --&gt; Proc[\"data/processed/{images,labels}&lt;br/&gt;YOLO layout + data.yaml\"]:::data\n\n  Proc --&gt; YOLOTrain[training/train.py&lt;br/&gt;Ultralytics YOLO26-pose]:::model\n  YOLOTrain --&gt; MainArt[\"artifacts/&amp;lt;run&amp;gt;/weights/best.pt\"]:::artifact\n  MainArt --&gt; Publish[scripts/publish_to_hf.py]:::code\n  Publish --&gt; HF[HF Hub&lt;br/&gt;kiselyovd/vehicle-keypoints]:::external\n\n  Raw --&gt; COCODS[CocoKeypointsDataset&lt;br/&gt;top-down crops + Gaussian heatmaps]:::code\n  COCODS --&gt; ViTTrain[training/train_vitpose.py&lt;br/&gt;Lightning + Hydra + MLflow]:::model\n  ViTTrain --&gt; BaseArt[artifacts/baseline/checkpoints/best.ckpt]:::artifact\n  BaseArt --&gt; Publish\n  Publish --&gt; HFBase[HF Hub&lt;br/&gt;baseline/ subdir]:::external\n\n  MainArt --&gt; Eval[evaluation/evaluate.py&lt;br/&gt;OKS-mAP + PCK]:::code\n  BaseArt --&gt; Eval\n  MainArt --&gt; API[FastAPI /detect]:::serve\n\n  classDef external fill:#FFF3E0,stroke:#EF6C00,color:#BF360C\n  classDef data fill:#FFE0B2,stroke:#E65100,color:#BF360C\n  classDef code fill:#FFCC80,stroke:#E65100,color:#BF360C\n  classDef model fill:#FFB74D,stroke:#BF360C,color:#fff\n  classDef artifact fill:#FF9800,stroke:#BF360C,color:#fff\n  classDef serve fill:#F57C00,stroke:#BF360C,color:#fff</code></pre>"},{"location":"architecture/#model-choice-rationale","title":"Model-choice rationale","text":"<p>Main \u2014 YOLO26-pose. Ultralytics' YOLO26-pose is the natural main for this task: it is a single-shot detector that jointly regresses bounding boxes, objectness, and a configurable number of keypoints per instance. The <code>kpt_shape=[14, 3]</code> hyperparameter cleanly accommodates non-human keypoint classes without any architectural surgery. Throughput on an RTX 3080 is an order of magnitude higher than top-down alternatives (no explicit crop step), and the produced <code>.pt</code> checkpoint plugs straight into the Ultralytics inference CLI, ONNX exporter, and Hub publishing flow. These operational wins matter as much as the accuracy numbers.</p> <p>Baseline \u2014 ViTPose-S. ViTPose is the academic reference implementation for transformer-based 2D pose estimation. Using the small variant (<code>ViTPose-S</code>, ~22 M params) pretrained on COCO human 17-keypoint gives us a concrete transfer-learning story: we replace the keypoint head with a fresh 14-channel deconv head and fine-tune. This provides a meaningful second number on the leaderboard and demonstrates that the repo is not locked into a single framework \u2014 the same evaluation protocol runs against both branches.</p> <p>Metrics \u2014 OKS-mAP + PCK. Object Keypoint Similarity mAP is the COCO-standard metric for keypoint tasks: it accounts for per-keypoint sigmas (tolerance) and instance scale, which makes comparison against published baselines meaningful. We pair it with PCK@0.05 (Percentage of Correct Keypoints within 5% of the bounding-box diagonal), which is intuitive to read at a glance \u2014 \"of all visible keypoints, how many did we get right?\" \u2014 and is robust to the absence of validated per-keypoint sigmas for the car class.</p>"},{"location":"serving/","title":"Serving","text":""},{"location":"serving/#local","title":"Local","text":"<pre><code>uv run uvicorn vehicle_keypoints.serving.main:app --reload\n</code></pre> <p>The app reads <code>MODEL_CHECKPOINT</code> from the environment (default <code>artifacts/best.pt</code>) at startup \u2014 set it to your trained weights before launching. Without a checkpoint on disk the app still boots but <code>/detect</code> returns 503.</p>"},{"location":"serving/#docker","title":"Docker","text":"<pre><code>docker compose up api\n</code></pre> <p>Image: <code>ghcr.io/kiselyovd/vehicle-keypoints</code>. The container honours the same <code>MODEL_CHECKPOINT</code> env var; if unset, the entrypoint falls back to the bundled weights baked into the image at build time (or to the HF Hub download if neither is present).</p>"},{"location":"serving/#endpoints","title":"Endpoints","text":"Method Path Description <code>GET</code> <code>/health</code> Liveness check \u2014 returns <code>{\"status\": \"ok\"}</code> <code>POST</code> <code>/detect</code> Run keypoint detection on one image <code>GET</code> <code>/metrics</code> Prometheus metrics (requests, latency, in-flight)"},{"location":"serving/#input-format","title":"Input format","text":"<p><code>POST /detect</code> takes a multipart upload of a single RGB JPEG or PNG, max ~10 MB. The backing YOLO26-pose model resizes to 640\u00d7640 internally (letterbox), so you do not need to pre-resize \u2014 the returned <code>image_width</code> / <code>image_height</code> always reflect the original upload dimensions and keypoint coordinates are rescaled back into that original frame.</p>"},{"location":"serving/#headers","title":"Headers","text":"<ul> <li><code>X-Request-ID</code> \u2014 echoed if the client sends one, otherwise minted server-side; propagate it from your upstream gateway for end-to-end traces.</li> <li><code>X-Detections</code> \u2014 present on PNG overlay responses; integer count of detected vehicles in the image.</li> </ul>"},{"location":"serving/#json-response-schema","title":"JSON response schema","text":"<p>Default (or when <code>Accept: application/json</code>):</p> <pre><code>{\n  \"detections\": [\n    {\n      \"bbox\": [x, y, w, h],\n      \"keypoints\": [\n        {\"x\": 123.4, \"y\": 456.7, \"v\": 2}\n      ],\n      \"score\": 0.91\n    }\n  ],\n  \"image_width\": 1920,\n  \"image_height\": 1080,\n  \"request_id\": \"01H...\"\n}\n</code></pre> <ul> <li><code>bbox</code> is xywh in the original image frame.</li> <li><code>keypoints</code> is always a list of 14 entries in the canonical CarFusion order (wheels, head-/tail-lights, exhaust, roof corners, body centre \u2014 see <code>CARFUSION_KEYPOINT_NAMES</code> in <code>vehicle_keypoints.inference.overlay</code>). Missing keypoints carry <code>v=0</code> and meaningless <code>x</code>/<code>y</code>.</li> <li><code>score</code> is the per-instance detection confidence.</li> </ul>"},{"location":"serving/#example-curl","title":"Example curl","text":"<p>JSON response:</p> <pre><code>curl -X POST -F \"file=@car.jpg\" http://localhost:8000/detect\n</code></pre> <p>PNG overlay response:</p> <pre><code>curl -X POST -F \"file=@car.jpg\" \\\n  -H \"Accept: image/png\" \\\n  -o out.png \\\n  http://localhost:8000/detect\n</code></pre> <p>The PNG return draws boxes, keypoint dots, and the car skeleton (edges defined in <code>vehicle_keypoints.inference.skeleton</code>) on a copy of the uploaded image.</p>"},{"location":"training/","title":"Training","text":""},{"location":"training/#prerequisites","title":"Prerequisites","text":"<pre><code>uv sync --all-groups\nbash scripts/sync_data.sh            # CarFusion COCO export -&gt; data/raw/\nuv run python -m vehicle_keypoints.data.prepare \\\n  --raw data/raw --out data/processed\n</code></pre> <p>Hardware: an RTX 3080 (10 GB VRAM) or better is recommended. The YOLO branch fits in ~6 GB at the default batch size; the ViTPose branch uses ~4 GB. Both branches expect <code>data/raw/</code> populated (CarFusion annotations + images) and <code>data/processed/</code> generated by <code>prepare.py</code> for the YOLO layout.</p>"},{"location":"training/#main-yolo26-pose","title":"Main \u2014 YOLO26-pose","text":"<pre><code>uv run python -m vehicle_keypoints.training.train +experiment=sota trainer.max_epochs=100\n</code></pre> <p>Expected wall time: ~60\u2013120 minutes on an RTX 3080 (batch 16, 640\u00b2, 100 epochs). Checkpoints land in <code>artifacts/&lt;run&gt;/weights/best.pt</code>. Ultralytics writes its own <code>results.csv</code> and TensorBoard event files alongside \u2014 point TensorBoard at <code>artifacts/</code> to browse live.</p>"},{"location":"training/#baseline-vitpose-s","title":"Baseline \u2014 ViTPose-S","text":"<pre><code>uv run python -m vehicle_keypoints.training.train_vitpose \\\n  model=baseline \\\n  data.vitpose.train_images=data/raw/train \\\n  data.vitpose.train_annotations=data/raw/annotations/car_keypoints_train.json \\\n  data.vitpose.val_images=data/raw/train \\\n  data.vitpose.val_annotations=data/raw/annotations/car_keypoints_train.json \\\n  trainer.max_epochs=30 \\\n  trainer.output_dir=artifacts/baseline\n</code></pre> <p>Expected wall time: ~30\u201360 minutes on an RTX 3080 (batch 32, 256\u00d7192 top-down crops, 30 epochs). Checkpoint written to <code>artifacts/baseline/checkpoints/best.ckpt</code>.</p>"},{"location":"training/#mlflow-tracking","title":"MLflow tracking","text":"<p>MLflow is wired into the ViTPose branch only (<code>mlflow ui --backend-store-uri ./mlruns</code>, browse at http://localhost:5000). Every Hydra run is one MLflow run with the resolved config logged as params and <code>train/loss</code>, <code>val/loss</code>, <code>val/pck</code>, <code>val/oks</code> as metrics.</p> <p>Ultralytics (the YOLO branch) does not use MLflow by default \u2014 it emits its own <code>results.csv</code> + TensorBoard logs under <code>artifacts/&lt;run&gt;/</code>. This split is intentional: MLflow would mostly duplicate what Ultralytics already provides for YOLO runs, and keeps the YOLO command free of unnecessary plumbing.</p>"},{"location":"training/#hydra-overrides-vitpose-branch","title":"Hydra overrides (ViTPose branch)","text":"Override Effect <code>trainer.max_epochs=50</code> Longer training <code>data.batch_size=16</code> Smaller batches for low-VRAM cards <code>model.lr=1e-4</code> Different learning rate <p>Multi-run sweep example:</p> <pre><code>uv run python -m vehicle_keypoints.training.train_vitpose -m \\\n  model.lr=1e-5,3e-5,1e-4 \\\n  data.batch_size=16,32\n</code></pre>"},{"location":"training/#gpu-memory","title":"GPU memory","text":"Branch Config Peak VRAM YOLO26-pose (<code>n</code> size) batch 16 \u00d7 640\u00b2 ~6 GB ViTPose-S batch 32 \u00d7 256\u00d7192 ~4 GB <p>Reduce <code>data.batch_size</code> (or <code>trainer.batch=</code> for YOLO) if you hit OOM on a smaller card.</p>"},{"location":"training/#transfer-learning-note-vitpose","title":"Transfer-learning note (ViTPose)","text":"<p>ViTPose-S ships pretrained on the COCO human 17-keypoint task. We re-head it to 14 car keypoints by replacing the final deconv layer with a fresh 14-channel head. Expect the first few epochs to show unstable loss while the new head learns from random init \u2014 this is normal. The backbone transfers well (car textures are within-distribution for a COCO-pretrained ViT), but the head must start from scratch, so don't early-stop too aggressively.</p>"}]}